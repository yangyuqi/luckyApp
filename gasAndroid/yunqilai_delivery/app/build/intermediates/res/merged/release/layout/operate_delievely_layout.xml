<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    >
    <LinearLayout
        android:layout_alignParentBottom="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingBottom="22dp"
        >


    <ListView
        android:id="@+id/ls_com"
        android:layout_above="@+id/tv_pay_now"
        android:dividerHeight="@dimen/space_divider_1"
        android:divider="@color/divider_color_page"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"></ListView>

        <TextView
            android:layout_alignParentBottom="true"
            android:id="@+id/tv_change_now"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:background="@drawable/tv_pay_bg"
            android:clickable="true"
            android:gravity="center"
            android:text="确定"
            android:textColor="@color/bg_white"
            android:textSize="@dimen/text_size_16" />
</LinearLayout>

</RelativeLayout>